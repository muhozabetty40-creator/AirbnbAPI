export const getUserProfile = (_req, res, _next) => {
    res.status(410).json({ message: "Profile endpoint removed" });
};
export const createUserProfile = (_req, res, _next) => {
    res.status(410).json({ message: "Profile endpoint removed" });
};
export const updateUserProfile = (_req, res, _next) => {
    res.status(410).json({ message: "Profile endpoint removed" });
};
